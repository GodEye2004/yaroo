version: "3.9"

services:
  backend:
    build: ./backend
    container_name: fastapi_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - app_net

networks:
  app_net:
    driver: bridge
